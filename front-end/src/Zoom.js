// import { useState, useEffect } from 'react';

const Zoom = () => {
    /*
    const [data , setData] = useState("");
    
    const restEndpoint = "http://localhost:4000/";
    
    useEffect(() => {
        fetch('http://localhost:4000/')
        .then(response => {
            return response.json();
        })
        .then(data => {
            setData(JSON.stringify(data, null, 2));
            console.log(data);
        });
    }, []);

    */
     return (
       <div>
           <p>hello</p>
           {/* data && <pre>{data}</pre> */}
       </div>
       
    );
}
 
export default Zoom;

